import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainView: {
    alignItems: 'center',
    paddingVertical: 10,
  },
  inputView: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  titleText: {
    fontSize: 40,
    textAlign: 'center',
  },
  input: {
    fontSize: 35,
  },
  errorText: {
    color: 'red',
  },
});
